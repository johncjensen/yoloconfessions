<script type="text/javascript">
$(document).ready(function() {
    $('.share-button').share({
        url: '<%= post_url(@post) %>',
        flyout: 'top center',
        twitter: {
            text: '<%= raw @post.title %> #yoloconfessions',
            link: '<%= post_url(@post) %>'
        },
        facebook: {
            text: '<%= raw @post.title %>  #yoloconfessions',
            link: '<%= post_url(@post) %>'
        }
    })
});
</script>
